# Quiche Engine 集成方案 - 快速参考卡片

## 📌 一句话总结

**推荐采用独立库模式，将代码移至 `quiche/api/`，使用 CMake 构建，与 libquiche 分离但协同工作。**

---

## 🎯 核心决策

```
问题: 是否将 C++ Engine 集成到 libquiche？

答案: ❌ 不建议直接集成

推荐: ✅ 独立库模式 (libquiche_engine)
```

---

## ⚡ 5 分钟速览

### 为什么不直接集成？

| 问题 | 影响 |
|------|------|
| 🔴 构建系统冲突 | Rust (Cargo) vs C++ 混合极度复杂 |
| 🔴 运行时依赖 | libev 污染核心库 |
| 🔴 ABI 不稳定 | C++ ABI 破坏跨平台兼容性 |
| 🔴 线程模型冲突 | 与 Rust 异步生态冲突 |
| 🔴 维护噩梦 | 技术栈混合，调试困难 |

### 为什么选择独立库？

| 优势 | 说明 |
|------|------|
| ✅ 架构清晰 | libquiche (Rust) + libquiche_engine (C++) |
| ✅ 构建简单 | 各自使用最合适的工具 (Cargo + CMake) |
| ✅ 依赖隔离 | libev 不影响核心 |
| ✅ 低风险 | 独立演进，互不干扰 |
| ✅ 易维护 | 职责分离，调试清晰 |

---

## 📂 目标目录结构

```
quiche/api/              ⭐ 新增 C++ Engine
├── include/
│   └── quiche_engine.h  (公共 API)
├── src/
│   ├── quiche_engine_impl.h
│   ├── quiche_engine_impl.cpp
│   ├── quiche_engine_api.cpp
│   ├── thread_utils.h
│   └── thread_utils.cpp
├── CMakeLists.txt       (构建配置)
├── README.md            (使用文档)
└── examples/            (示例代码)
```

---

## 🚀 快速实施步骤

```bash
# 步骤 1: 创建目录
mkdir -p quiche/api/{include,src}

# 步骤 2: 移动文件
cd quiche/examples/quic-demo/src
mv quiche_engine.h ../../../api/include/
mv quiche_engine_impl.* ../../../api/src/
mv quiche_engine_api.cpp ../../../api/src/
mv thread_utils.* ../../../api/src/

# 步骤 3: 创建 CMakeLists.txt
# (见详细文档)

# 步骤 4: 构建测试
cd quiche/api
mkdir build && cd build
cmake ..
make

# 步骤 5: 运行测试
ctest
```

---

## 📊 方案对比一览表

| 维度 | 方案 A<br>(核心集成) | 方案 C<br>(独立库) ✅ |
|------|---------------------|----------------------|
| **风险** | 🔴 极高 (9/10) | 🟢 低 (2/10) |
| **复杂度** | 🔴 极高 | 🟢 低 |
| **工期** | 3-4 周 | 1-2 周 |
| **维护成本** | 🔴 极高 (30-40%) | 🟢 低 (5-10%) |
| **ABI 稳定性** | ❌ 破坏 | ✅ 不影响 |
| **依赖隔离** | ❌ libev 污染核心 | ✅ 清晰隔离 |
| **社区接受** | 🔴 极低 | 🟢 高 |
| **评分** | 1.7/10 | **9.1/10** |

---

## 🎬 实施时间线

```
第 1 天   创建目录 + 文档准备
第 2 天   移动文件 + 代码改进
第 3-4 天 CMake 构建系统
第 5-6 天 测试和跨平台验证
第 7 天   文档完善 + 发布
```

**总计**: 7 个工作日

---

## ✅ 验收标准

### 必须达到
- ✅ 所有平台编译通过 (Linux, macOS, iOS, Android)
- ✅ 测试覆盖率 > 80%
- ✅ 零内存泄漏
- ✅ 性能开销 < 5%
- ✅ API 文档完整

### 建议达到
- 🎯 示例代码可直接运行
- 🎯 10 分钟内上手
- 🎯 跨编译器兼容 (GCC, Clang, MSVC)

---

## 📚 相关文档

| 文档 | 用途 | 受众 |
|------|------|------|
| `EXECUTIVE_SUMMARY_CN.md` | 快速决策参考 | 管理层 |
| `QUICHE_ENGINE_INTEGRATION_PROPOSAL.md` | 完整技术方案 | 技术团队 |
| `IMPLEMENTATION_CHECKLIST.md` | 实施跟踪清单 | 开发团队 |
| `QUICK_REFERENCE.md` (本文) | 快速参考 | 所有人 |

---

## 🔑 关键决策点

### Q1: 是否将 C++ 代码打包进 libquiche？
**A**: ❌ 不推荐。理由：架构冲突，维护困难，风险极高。

### Q2: 代码应该放在哪里？
**A**: ✅ `quiche/api/` 目录，与核心分离但保持在同一仓库。

### Q3: 如何构建？
**A**: ✅ 使用 CMake 独立构建，生成 `libquiche_engine.a/so`。

### Q4: 如何分发？
**A**: ✅ 与 libquiche 分开分发，用户按需选择。

### Q5: 依赖如何处理？
**A**: ✅ libquiche_engine 依赖 libquiche + libev，通过 pkg-config 管理。

---

## 🛡️ 风险缓解

| 风险 | 缓解措施 |
|------|---------|
| 依赖查找失败 | 详细安装指南 + 手动路径配置 |
| 跨平台问题 | 早期多平台测试 + CI/CD |
| 性能回归 | 基准测试 + 持续监控 |
| API 不满足需求 | 用户反馈 + 预留扩展点 |

---

## 💡 业界参考

成功案例（核心库 + 高层包装）：

1. **libcurl + curlcpp**
2. **SQLite + sqlite_modern_cpp**
3. **gRPC 多语言绑定**
4. **OpenSSL + Chromium 包装**

---

## 📞 需要帮助？

- 📖 查看完整方案: `QUICHE_ENGINE_INTEGRATION_PROPOSAL.md`
- ✅ 查看实施清单: `IMPLEMENTATION_CHECKLIST.md`
- 🚀 查看执行摘要: `EXECUTIVE_SUMMARY_CN.md`
- 💬 提交 Issue: GitHub Issues

---

## 🎯 下一步行动

**立即开始**:
```bash
cd quiche/examples/quic-demo
cat EXECUTIVE_SUMMARY_CN.md          # 阅读执行摘要
cat IMPLEMENTATION_CHECKLIST.md      # 阅读实施清单
# 开始阶段 1: 创建目录结构
```

**决策流程**:
```
1. 阅读执行摘要 (5 分钟)
2. 决策: 采用方案 C
3. 阅读实施清单 (10 分钟)
4. 启动实施 (7 天)
```

---

**版本**: 1.0
**日期**: 2025-11-06
**状态**: ✅ 推荐实施
